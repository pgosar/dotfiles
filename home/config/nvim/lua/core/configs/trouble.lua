require("trouble").setup({
  preview = {
    type = "split",
    relative = "win",
    position = "right",
    size = require("defaults").plugin_settings.trouble_preview_size,
  },
  focus = true,
})
